This script cleans time steps older than date in all ts in groupPath recursively.
It is useful to keep only a time window of times steps in the database
and avoids to let the size of the database grow over time.
