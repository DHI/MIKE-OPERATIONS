This script contains two functions

- MakeSpreadsheet - makes a new spreadsheet in MIKE CUSTOMISED, adds a sheet, writes a value in a cell and saves the spreadsheet to database.
- UpdateSpreadsheet - finds, opens modifies and saves and existing spreadsheet.

To run - simply copy the code into Script Editor, save and run the two functions
