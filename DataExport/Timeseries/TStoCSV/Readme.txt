Export TS to CSV.

Functions:

ExportTs2Csv(ts, csvFilePath, stationId, stationName, timeOfSimulationRun) -  Saves a time series to a CSV file.
ExportTsGroup2Csv(tsGroupPath, csvDestinationDirectory, realtimeConfigurationName) - Saves all time series in a time series group to CSV files.
